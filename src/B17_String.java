public class B17_String {
    static void main() {
        // Nhom phuong thuc voi String:
        /*
        1. s1.equals(s2)    //so sanh 2 chuoi co bang nhau khong, co ther dung .equalsIgnoreCase()
        2. s.split()    //tra ve String[] dua tren ky tu split, can than voi . + -
        3. s.toCharArray() // dua tung ki tu thanh phan tu trong array
        4. s.toUpperCase()
        5. s.toLowerCase()
        6. s.contains()     // Boolean
        7. s.trim()     // == s.strip()
        8. s.replace("old", "new")
         */

        // Nhom phuong thuc voi StringBuilder

        /*
        // Khoi tao: StringBuilder <name> = new StringBuilder("origin")
        1. s.append("dulieu")   //du lieu co the tuy y
        2. s.insert(index, value)
        3. s.delete(start, end)
        4. s.reverse()
        5. s.toString()     // dua tro lai chuoi string binh thuong

         */

        // Nhom ca 2 deu dung duoc:
        /*
        1. .length()
        2. s.charAt(index)  //truy cap
        3.substring(start, end)     // luon tra ve string
        4. s.indexOf("str")     // == find()

         */
    }
}
